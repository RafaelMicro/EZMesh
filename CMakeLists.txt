cmake_minimum_required(VERSION 3.10)
project(RAFAEL_HOST_SDK LANGUAGES C CXX)
list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/cmake")
list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/cmake/modules")
if(${CONFIG_USE_CROSS_COMPILER})
    list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/cmake/cross_compile")
endif()
include(extension)

ext_include(config)
ext_include_ifdef(CONFIG_USE_CROSS_COMPILER toolchain)
ext_include(project)

ext_add_subdirectory(third_party)
ext_add_subdirectory(module)
ext_add_subdirectory_ifdef(CONFIG_GEN_SYSTEM integrate)
